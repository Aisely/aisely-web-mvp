"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var winston_1 = require("winston");
var logger = winston_1.createLogger({
    level: process.env.NODE_ENV === 'production' ? 'info' : 'debug',
    format: winston_1.format.combine(winston_1.format.colorize(), winston_1.format.label({ label: 'REDIS REALTIME' }), winston_1.format.timestamp({
        format: 'YYYY-MM-DD HH:mm:ss',
    }), winston_1.format.json(), winston_1.format.printf(function (info) {
        // @ts-ignore
        var args = info[Symbol.for('splat')];
        var strArgs = args === null || args === void 0 ? void 0 : args.map(function (a) { return JSON.stringify(a); }).join(' ');
        return "[" + info.timestamp + "] [" + info.level + "]: " + info.message + " " + (strArgs || '') + " | " + info.label;
    })),
    transports: [new winston_1.transports.Console()],
});
exports.default = logger;
//# sourceMappingURL=logger.js.map